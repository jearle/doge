// import { fromJS } from 'immutable';
// import { makeSelectMyDojoPageDomain } from '../selectors';

// const selector = makeSelectMyDojoPageDomain();

describe('makeSelectMyDojoPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
