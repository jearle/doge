// import { fromJS } from 'immutable';
// import { makeSelectChatPageDomain } from '../selectors';

// const selector = makeSelectChatPageDomain();

describe('makeSelectChatPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
