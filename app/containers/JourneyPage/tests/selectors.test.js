// import { fromJS } from 'immutable';
// import { makeSelectJourneyPageDomain } from '../selectors';

// const selector = makeSelectJourneyPageDomain();

describe('makeSelectJourneyPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
