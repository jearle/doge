// import { fromJS } from 'immutable';
// import { makeSelectHabitPageDomain } from '../selectors';

// const selector = makeSelectHabitPageDomain();

describe('makeSelectHabitPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
