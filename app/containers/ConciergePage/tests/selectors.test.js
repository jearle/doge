// import { fromJS } from 'immutable';
// import { makeSelectConciergePageDomain } from '../selectors';

// const selector = makeSelectConciergePageDomain();

describe('makeSelectConciergePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
